/**
 * Created by iliyan on 9/2/15.
 */
describe("knobs", function () {

    beforeEach(module('knobs'));

    it("should be available", inject([function ($window) {
        //expect(knobs).toBeDefined();
    }]));

});
